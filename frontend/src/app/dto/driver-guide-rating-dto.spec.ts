import { DriverGuideRatingDto } from './driver-guide-rating-dto';

describe('DriverGuideRatingDto', () => {
  it('should create an instance', () => {
    expect(new DriverGuideRatingDto()).toBeTruthy();
  });
});
