import { HotelUserRatingDTO } from './hotel-user-rating-dto';

describe('HotelUserRatingDTO', () => {
  it('should create an instance', () => {
    expect(new HotelUserRatingDTO()).toBeTruthy();
  });
});
