import { DriverGuidePackage } from './driver-guide-package';

describe('DriverGuidePackage', () => {
  it('should create an instance', () => {
    expect(new DriverGuidePackage()).toBeTruthy();
  });
});
